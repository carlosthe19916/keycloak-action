on:
  push:
    branches:
      - "master"
  pull_request:
    branches:
      - "master"

jobs:
  test:
    uses: ./.github/workflows/keycloak.yaml
    strategy:
      matrix:
        keycloak_version: [latest]
      fail-fast: false
    with:
      keycloak_version: ${{ matrix.keycloak_version }}
